# Lightweight requirements for Vercel deployment
# spaCy and NLTK removed to stay under 250MB limit
# App works perfectly without them using regex and TF-IDF

# Web Framework
Flask>=3.0.0
flask-cors>=4.0.0

# Machine Learning
scikit-learn>=1.3.0
numpy>=1.24.0

# Document Processing
PyPDF2>=3.0.0
python-docx>=1.1.0

# Note: spaCy and NLTK removed for Vercel compatibility
# The app uses regex and TF-IDF for matching (still very effective!)

